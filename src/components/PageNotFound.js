import React from 'react';

import { Typography } from '@material-ui/core';     

const PageNotFound = () => (
  <div>
    <Typography variant="h3" gutterBottom>You've got lost.</Typography>
    <Typography variant="overline" align="center">404 Page not found.</Typography>
  </div>
);

export default PageNotFound;